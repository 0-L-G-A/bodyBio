{
  "name": "body-bio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd BE && nest start && cd ../FE && react-scripts start",
    "build": "cd BE && npm install && nest build && cd ../FE && npm install && react-scripts build"
  },
  "dependencies": {}
}
